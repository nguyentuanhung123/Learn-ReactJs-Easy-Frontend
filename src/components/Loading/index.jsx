import React from 'react';

import PropTypes from 'prop-types';

import './styles.scss';


const Loading = (props) => {
    return (
        <div>Loading</div>
    )
}

export default Loading;

Loading.propsTypes = {

}
